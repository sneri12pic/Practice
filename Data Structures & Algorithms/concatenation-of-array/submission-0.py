class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []
        i = 2
        for num in nums:
            ans.append(num)
        else:
            for num in nums:
                ans.append(num)
        return ans

